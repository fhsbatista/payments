services:
  db:
    image: mysql:5.7
    container_name: authorization-db
    restart: always
    ports:
      - "3306:3306"
    environment:
      - MYSQL_DATABASE=test
      - MYSQL_ROOT_PASSWORD=root